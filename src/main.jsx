import React from "react";
import ReactDOM from "react-dom/client";
import GuildTracker from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuildTracker />
  </React.StrictMode>
);
